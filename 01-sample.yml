- name: Demo
  hosts: all
  tasks:
    - debug:
        msg: "Hello"

- name: Print Fact
  hosts: localhost
  connection: local
  tasks:
    ansible.builtin.debug:
    msg: “{{ ansible_architecture }}”